# Primary Gemini API Key (Required)
GEMINI_API_KEY=your_gemini_api_key_here

# Backup API key environment variables (Optional - for additional security)
GOOGLE_AI_API_KEY=your_gemini_api_key_here
GENERATIVE_AI_KEY=your_gemini_api_key_here

# Application Environment
NODE_ENV=production
NEXT_PUBLIC_APP_ENV=production

# Security Configuration
ENABLE_API_KEY_VALIDATION=true
ENABLE_RATE_LIMITING=true

# Instructions:
# 1. Get your Gemini API key from: https://makersuite.google.com/app/apikey
# 2. Replace 'your_gemini_api_key_here' with your actual API key
# 3. Keep this file secure and never commit it to version control
# 4. For production, set these in your Vercel environment variables

# Twitter credentials are entered by users in the app settings
# They are processed securely server-side and never stored permanently
